{
  "name": "claude-channel-mattermost",
  "version": "0.0.10",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "claude-mattermost-channel": "./server.ts",
    "claude-mattermost-access": "./access-cli.ts"
  },
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "check": "bun build ./server.ts --target=bun --outfile=/tmp/mm-channel-check.js && rm -f /tmp/mm-channel-check.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "mattermost-shared": "workspace:*"
  }
}

{
  "name": "claude-channel-mattermost",
  "version": "0.0.4",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "claude-channel-mattermost": "./server.ts",
    "claude-mattermost-channel": "./server.ts",
    "codex-mattermost-channel": "./codex-server.ts",
    "codex-mattermost-access": "./codex-access-cli.ts"
  },
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "start:claude": "bun install --no-summary && bun server.ts",
    "start:codex": "bun install --no-summary && bun codex-server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@openai/codex-sdk": "^0.142.4"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
